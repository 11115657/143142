export class PostProcessing {
  // Lightweight placeholder: WebGL post-processing can be added here later
  // without changing Game.render(). Current MVP uses tone mapping + CSS damage vignette.
  enabled = true
}
